# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

На момент создания этого файла репозиторий пуст: содержит только `README.md` с одной строкой `# mcp-jira`. Исходного кода, файлов сборки (`go.mod`, `Makefile`, и т.п.), тестов, а также Cursor/Copilot-правил в репозитории нет, поэтому задокументировать команды сборки/тестов и архитектуру пока невозможно.

Имя репозитория и путь (`go/src/github.com/grevus/mcp-jira`) предполагают Go-проект MCP-сервера для Jira, но это не подтверждено содержимым — не опирайся на это как на факт.

## Когда появится код

Обнови этот файл, добавив:
- команды сборки, запуска и тестирования (включая запуск одиночного теста);
- точку входа MCP-сервера и способ его регистрации/конфигурации;
- высокоуровневую архитектуру (слои, ключевые пакеты, интеграция с Jira API), которую нельзя быстро понять из одного файла.
